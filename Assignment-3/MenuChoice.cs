enum MenuChoice
{
    Addition = 1,
    Subtraction = 2,
    Multiplication = 3,
    Division = 4,
    Modulus = 5,
    Power = 6,
    ClearHistory = 7
}
// The MenuChoice enum represents the different operations available in the calculator application. Each operation is assigned a unique integer value, starting from 1 for Addition and incrementing by 1 for each subsequent operation. The ClearHistory option is also included as the last choice in the menu.